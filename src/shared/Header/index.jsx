import React from 'react';
//styles
import styles from './styles.module.scss';

const Header = () => {
	return (
		<div className={styles.wrapper}>
			<h2 className={styles.title}>Створення персони</h2>
		</div>
	);
};

export default Header;
